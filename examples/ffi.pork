func malloc(size)
  native ffi "c:malloc:void*"

func free(pointer)
  native ffi "c:free:void"

export func main() {
  while true {
    let pointer = malloc(8192)
    println(pointer)
    free(pointer)
  }
}
