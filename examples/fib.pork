/* fibonacci sequence */
func fib(n) {
  if n == 0 {
    0
  } else {
    if n == 1 {
      1
    } else {
      fib(n - 1) + fib(n - 2)
    }
  }
}

export func main() {
  let result = fib(20)
  println(result)
}
